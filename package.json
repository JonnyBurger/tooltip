{
  "version": "1.0.0-alpha.4",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "watch": "lerna run watch --stream --parallel"
  },
  "devDependencies": {
    "lerna": "^2.2.0"
  }
}
